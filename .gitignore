# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
*.egg-info/
dist/
build/
.eggs/
*.egg
pip-wheel-metadata/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# uv
uv.lock

# Environment
.env
.env.local
.env.*.local

# GDS / EDA outputs
*.gds
*.gds2
*.oas
*.mag
*.spef
*.sdf
/tmp/gelochip_output/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Logs
*.log
logs/

# KLayout
*.lyt
*.lym

# SPICE
*.raw
*.lis
*.nutbin
*.nutascii

# OS
.DS_Store
Thumbs.db
*.swp
*.swo

# IDE
.vscode/settings.json
.idea/

# Vector stores (large binary files)
data/*/faiss_index/
data/**/*.pkl
data/**/*.index

# ── Model weights & large datasets (download from Drive — see README) ─────────
*.pt
*.pth
*.bin
*.safetensors
*.gguf
*.ckpt
*.onnx
*.mat
*.npz
# keep the small (216 KB) PPO policy tracked (comment on its own line — gitignore
# does not allow trailing comments on a pattern):
!models/layoutrl/drc_corrector_ppo.pt
# keep the embedding model (87 MB safetensors, < GitHub's 100 MB limit) tracked
# so the RAG stack runs fully offline with no HuggingFace download:
!models/embeddings/all-MiniLM-L6-v2/model.safetensors

# Chainlit
.chainlit/
.files/

# ── EDA scratch / Magic + Netgen extraction artifacts (never commit) ──────────
*_drc_out/
*_lvs_out/
*_pex_out/
*.ext
*.nodes
*.res.ext
*.rpt
els
/*.gds
/*.spice
outputs/

# ChromaDB vector store — auto-rebuilt by ./scripts/run.sh on first launch
data/chroma_db/
data/sessions.db

# staged minimal gf180 PDK for the Docker build (generated by scripts/stage_pdk.sh)
/pdk/
.drcwork/

!Chipathon2025_2_padring.gds
# ALIGN integration — materialized by notebooks/align/setup_align.sh, not committed.
# Tracked: src/, *.py, *.ipynb, setup_align.sh, README.md, netlists/
notebooks/align/.venv-align/
notebooks/align/.align-build/
notebooks/align/ALIGN-public/
notebooks/align/.deps/
notebooks/align/pdks/*
!notebooks/align/pdks/GF180_PDK
notebooks/align/pdks/GF180_PDK/**/__pycache__/
notebooks/align/examples/
notebooks/align/datasets/
notebooks/align/runs/
notebooks/align/out/
report/D5_Gelochip_IRIS.gds